<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Layout Testing</title>
  <style>
    body {
      background: black;
      color: white;
      font-family: monospace;
    }
    .initial-box {
      background-color: blue;
      border: 4px solid white;
      padding: 3px;
      margin: 3px;
      position: absolute;
      font-size: 16px;
    }
  </style>
</head>
<body>
  <div 
    id="box"
    class="initial-box"
  >
  test
  </div>
  <script src="file://wsl.localhost/Ubuntu-24.04/home/erohman/ui_layout/main.js"></script>
</body>
</html>



function resetLayout() {
    const width = window.innerWidth;
    const height = window.innerHeight;
    const rand = Math.random();
    // console.log(rand);
    const box = document.getElementById('box');
    console.log("width: ", width);
    const flooredWidth = Math.floor(width / 10) * 10;
    console.log("floored width: ", flooredWidth);


    let x = 500;
    let y = 500;
    box.style.left = x + 'px';
    box.style.top = y + 'px';
}

resetLayout();
window.addEventListener('resize', resetLayout);
